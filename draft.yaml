models:
  # ...
  Category:
      name: string:100
      description: string
      user_id: id foreign
      relationships:
        hasMany: Product

  Product:
      name: string:100
      description: string
      price: integer
      category_id: id foreign
      user_id: id foreign
      relationships:
        belongsTo: Category, User

  User:
      name: string:100
      email: string
      relationships: 
        hasMany: Product, Category

controllers:
  # ...
  Category:
    resource: api
  
  Product:
    resource: api

seeders: Category, Product